import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminProducts: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">AdminProducts</Typography>
      <Typography>AdminProducts functionality coming soon...</Typography>
    </Box>
  );
};

export default AdminProducts;
